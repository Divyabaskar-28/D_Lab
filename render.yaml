services:
  - type: web
    name: subtitle-voice
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      apt-get update
      apt-get install -y ffmpeg
    startCommand: gunicorn run:app